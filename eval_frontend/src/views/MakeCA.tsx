import React, { useState } from "react";
import { Form } from "antd";

export default () => {
  return (
    <div>
      <Form>
        <Form.Item>Make an assigment in minutes!</Form.Item>
      </Form>
    </div>
  );
};
